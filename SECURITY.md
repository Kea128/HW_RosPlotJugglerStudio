# 安全策略

## 支持范围

安全修复优先提供给最新 `stable` 版本；最新 `beta` 仅用于提前验证，不保证长期
支持。更早版本可能只收到升级建议。

## 报告漏洞

请使用 GitHub Security Advisory 的私密报告功能联系维护者，不要先创建公开
Issue。报告应包含受影响版本、复现条件、影响、最小复现和建议缓解方式，但不要
附带真实凭证、私钥或敏感用户数据。维护者将在合理时间内确认收到，并在验证、
修复和发布协调完成后共同确定披露时间。

## 发布与密钥

发布资产在 draft 阶段接受人工验收，发布包和更新 manifest 使用 SHA-256 复验。
签名私钥、证书密码、GitHub token 和其他凭证不得提交到仓库或写入日志/artifact；
应存放于组织密钥管理系统或受保护的 GitHub Environment secrets，并遵循最小
权限和定期轮换。发现泄露时立即吊销和轮换，不要只从后续提交中删除。

自动更新只接受既定 HTTPS GitHub 地址、严格 manifest schema、大小和 SHA-256
匹配的包。任何要求关闭 TLS 校验、放宽路径检查或让 PR 获得 release 写权限的
变更都应视为安全敏感变更并由第二位维护者审查。
